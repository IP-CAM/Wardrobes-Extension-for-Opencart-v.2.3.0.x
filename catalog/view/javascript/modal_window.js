$(document).ready(function () {    ////for telephone modal    //$('#telephone_button').click(function () {    //    var data = "";    //    $('.modal_window_box input').each(function (index, value) {    //        data += "&" + $(this).attr('name') + "=" + $(this).val();    //    });    //    alert(data);    //    ajaxClientCall(data);    //});    $('.popup .close_window, .overlay').click(function (){        $('.overlay').hide(40);        $('.popup').hide(400);    });});function ajaxClientCall(data) {    if(data == undefined) {        var button = $("button[data-modal], input[type='submit'][data-modal], input[type='button'][data-modal]");        var field = $("textarea[data-modal], input[type='text'][data-modal], input[type='hidden'][data-modal]");        data = "";        $(field).each(function (index, value) {            data += "&" + $(this).attr('name') + "=" + $(this).val();        });    }    if(data.indexOf('two_modal') == -1) {        data += '&two_modal=0';    }    $.ajax({        url: 'index.php?route=customer/customer/ajaxAddCustomer',        dataType: 'json',        data: data,        type: 'post',        beforeSend: function () {        },        success: function (json) {            var html = json['html'];            $('.popup.modal_window_box').empty();            $('.popup.modal_window_box').append(html);            $('.overlay').show(0);            $('.popup').show(400);        },        error: function (xhr, ajaxOptions, thrownError) {            alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);        }    });}